{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "ui/Dockerfile",
    "watchPatterns": ["ui/**", "package.json", "pnpm-workspace.yaml", "pnpm-lock.yaml"]
  },
  "deploy": {
    "healthcheckPath": "/"
  }
}
